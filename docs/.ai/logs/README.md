# Work Logs

Store only logs that provide future value: complex investigations, experiments,
important interrupted work, or decisions whose history is useful.

Naming:

```text
YYYY-MM-DD-short-topic.md
```

Routine progress belongs in `active-context.md` and `progress.md`. Avoid creating a log
for every trivial edit.
