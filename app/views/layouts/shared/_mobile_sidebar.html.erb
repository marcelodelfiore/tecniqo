<div id="app-mobile-sidebar" class="hs-overlay hidden size-full fixed inset-0 z-60 overflow-y-auto pointer-events-none" role="dialog" tabindex="-1">
  <div class="hs-overlay-open:translate-x-0 -translate-x-full pointer-events-auto relative flex h-full w-72 flex-col bg-white border-r border-gray-200 transition-all duration-300">
    <div class="flex items-center justify-between border-b border-gray-200 px-4 py-4">
      <div>
        <h2 class="text-base font-semibold text-gray-900"><%= application_name %></h2>
        <p class="text-sm text-gray-500"><%= t("app.tagline") %></p>
      </div>

      <button
        type="button"
        class="inline-flex items-center justify-center rounded-lg border border-gray-200 bg-white p-2 text-gray-700"
        data-hs-overlay="#app-mobile-sidebar">
        <span class="sr-only"><%= t("nav.close_menu") %></span>
        ✕
      </button>
    </div>

    <nav class="space-y-1 px-4 py-4">
      <%= link_to t("nav.home"), root_path, class: nav_link_classes(root_path) %>

      <% if authenticated? %>
        <%= link_to t("nav.dashboard"), dashboard_path, class: nav_link_classes(dashboard_path) %>
        <% if policy(:my_work).show? %>
          <%= link_to t("nav.my_work"), my_work_index_path, class: nav_link_classes(my_work_index_path) %>
        <% end %>
        <% if policy(WorkOrder).index? %>
          <%= link_to t("nav.work_orders"), work_orders_path, class: nav_link_classes(work_orders_path) %>
        <% end %>
        <% if policy(Customer).index? %>
          <%= link_to t("nav.customers"), customers_path, class: nav_link_classes(customers_path) %>
        <% end %>
        <% if policy(ServiceType).index? %>
          <%= link_to t("nav.service_types"), service_types_path, class: nav_link_classes(service_types_path) %>
        <% end %>
        <% if policy(Invitation).index? %>
          <%= link_to t("nav.invitations"), invitations_path, class: nav_link_classes(invitations_path) %>
        <% end %>
        <% if policy(Membership).index? %>
          <%= link_to t("nav.members"), memberships_path, class: nav_link_classes(memberships_path) %>
        <% end %>
      <% end %>
    </nav>
  </div>
</div>
