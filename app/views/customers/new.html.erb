<% content_for :title, t("customers.new.title") %>
<div class="mx-auto max-w-2xl space-y-6">
  <%= render "shared/breadcrumbs", items: [ { label: t("customers.index.title"), path: customers_path }, { label: t("customers.new.title") } ] %>
  <%= render "shared/page_header", title: t("customers.new.title"), subtitle: t("customers.new.subtitle") %>
  <div class="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm sm:p-6"><%= render "form", customer: @customer %></div>
</div>
