<% flash.each do |type, message| %>
  <div class="mx-auto max-w-7xl px-4 pt-4">
    <div class="rounded-xl border px-4 py-3 text-sm <%= flash_classes(type) %>">
      <%= message %>
    </div>
  </div>
<% end %>
